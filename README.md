# AI News Intelligence Platform

Enterprise-grade FastAPI application scaffold for collecting and analyzing AI news sources.

This repository contains the minimal foundation (packages, configuration, and source registry) without any business logic, crawlers, endpoints, or database models.

See `app/config/settings.py` for configuration and `.env` for environment overrides.
